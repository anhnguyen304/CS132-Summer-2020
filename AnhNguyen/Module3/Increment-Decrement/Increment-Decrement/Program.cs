using System;

namespace Increment_Decrement
{
    class Program
    {
        static void Main(string[] args)
        {
            //for(int a = 0; a <= 10; a++) //Define an int variable ‘a’ and initialize it with 0.
                                          //the condition expression a <= 10, if this condition returns true then it will execute a code block.
                                         //After executing the code block, it will go to the iterator.
                                        //After each iteration of the loop, it increments a by 1 (a++),
                                       //the loop will stop when it meets the i <= 10
                                      //
            {
               //Console.WriteLine(a); //print out 0 to 10  
            }
            for (int b = 15; b > 0; b--)
            {
                Console.WriteLine(b);
            }
        }
    }
}
